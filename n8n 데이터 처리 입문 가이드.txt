n8n 데이터 처리 입문 가이드
1. JSON이란 무엇인가요?
JSON(JavaScript Object Notation)은 데이터를 저장하고 전달하는 형식입니다. 마치 메모장에 정보를 적는 것처럼, 컴퓨
터가 이해할 수 있는 방식으로 데이터를 정리합니다.

JSON의 기본 구조
{
"이름": "김철수",
"나이": 30,
"직업": "개발자"
}

위 예시에서:
{ } 중괄호로 데이터를 감쌉니다
"이름" , "나이" , "직업" 은 키(key)입니다
"김철수" , 30 , "개발자" 는 값(value)입니다
키와 값은 콜론( : )으로 연결됩니다
각 항목은 쉼표( , )로 구분합니다

2. 리스트(배열)란 무엇인가요?
리스트는 여러 개의 데이터를 순서대로 담는 상자입니다. [ ] 대괄호를 사용합니다.

간단한 리스트 예시
["사과", "바나나", "포도"]

JSON 객체들의 리스트
[
{
"이름": "김철수",
"나이": 30
},
{
"이름": "이영희",
"나이": 25
}
]

3. n8n의 아이템(Item)이란?
n8n에서 아이템은 하나의 데이터 덩어리입니다. 엑셀로 비유하면:
아이템 = 엑셀의 한 행(row)
아이템의 필드 = 엑셀의 열(column)

n8n 아이템의 실제 모습
{
"json": {
"이름": "김철수",
"이메일": "kim@example.com",
"가입일": "2024-01-15"
}
}
여기서 "json" 안에 실제 데이터가 들어있습니다.

4. n8n에서 여러 아이템 처리하기
n8n은 보통 여러 개의 아이템을 한꺼번에 처리합니다:
[
{
"json": {
"이름": "김철수",
"이메일": "kim@example.com"
}
},
{
"json": {
"이름": "이영희",
"이메일": "lee@example.com"
}
},
{
"json": {
"이름": "박민수",
"이메일": "park@example.com"
}
}
]

이것은 3개의 아이템(3명의 사용자 정보)을 담고 있는 리스트입니다.

5. 데이터 접근하기 - 기본 표현식
n8n에서는 {{ }} 이중 중괄호를 사용해 데이터에 접근합니다.

현재 아이템의 데이터 가져오기
{{ $json.이름 }} // "김철수"를 가져옴
{{ $json.이메일 }} // "kim@example.com"을 가져옴

실제 사용 예시
만약 이메일을 보내는 노드에서:
받는 사람: {{ $json.이메일 }}
제목: 안녕하세요, {{ $json.이름 }}님!
이렇게 설정하면 각 사람에게 맞춤형 이메일이 전송됩니다.

6. API란 무엇인가요?
API(Application Programming Interface)는 서로 다른 프로그램들이 대화하는 방법입니다.

일상생활 비유
API는 레스토랑의 웨이터와 같습니다:
손님(n8n): "김치찌개 1개 주세요"
웨이터(API): 주문을 주방에 전달
주방(서버): 김치찌개 조리
웨이터(API): 완성된 음식을 손님에게 전달

API 요청과 응답의 실제 모습
요청 (Request)
GET https://api.weather.com/seoul
Headers: {
"Authorization": "Bearer YOUR_API_KEY"
}

응답 (Response)
{
"city": "서울",
"temperature": 25,
"weather": "맑음",
"humidity": 60
}

7. API Key가 필요한 이유
API Key는 디지털 신분증과 같습니다.

API Key가 필요한 이유
1. 신원 확인: 누가 API를 사용하는지 확인
2. 사용량 제한: 무료 사용자는 하루 1000번, 유료는 무제한
3. 보안: 권한이 있는 사용자만 접근 허용
4. 과금: 사용한 만큼 요금 청구

8. n8n에서 API 사용하기
HTTP Request 노드 설정
1. Method: GET (데이터 가져오기) / POST (데이터 보내기)
2. URL: API 주소
3. Authentication: API Key 입력
4. Headers: 추가 정보 설정



n8n Self-hosted AI Starter Kit 설치 가이드

사전 준비
Docker Desktop 설치 n8n을 개인 PC 에서 실행하기 위해서는 Docker Desktop이 필요합니다.
Docker는 프로그램을 컨테이너라는 격리된 환경에서 실행할 수 있게 해주는 도구입니다.

macOS: https://www.docker.com/products/docker-desktop/ 에서 다운로드
Windows: 다운로드 후 설치할 때 "Use WSL 2 backend" 옵션을 반드시 체크하세요
설치 완료 후 Docker Desktop을 실행하고, Settings에서 메모리를 최소 4GB 이상 할당하세요

Windows에서 그래픽 카드(GPU) 확인하기 더 빠른 AI 처리를 위해 NVIDIA 그래픽 카드가 있는지 확인해
보겠습니다:
1. Ctrl + Shift + Esc 키를 눌러 작업 관리자를 엽니다
2. 성능 탭을 클릭합니다
3. 왼쪽에서 GPU 항목을 확인합니다
4. "NVIDIA GeForce"로 시작하는 이름이 보이면 GPU 모드를 사용할 수 있습니다

1단계: n8n 프로젝트 파일 다운로드
n8n 이 최신 버전에서 상당히 다양한 문제가 보여지고 있다고 합니다.
n8n 과 같은 솔루션은 최신 버전 보다는, 오히려 많은 사람들이 이미 사용한 기존 버전이 좋을 때가 꽤
있습니다.
따라서, 다음과 같이 최신 버전을 다운로드 받아 설치하시면 현재는 문제가 되고 있어서 부득이 다음
링크에서 ZIP 파일을 다운받으셔서

https://drive.google.com/file/d/1u77npubBobc-8VVug43Bi5fG5ZLkKt2j/view?us
p=sharing

다운로드 받은 ZIP 파일을 n8n 을 관리하실 폴더(윈도우 예: C:\Users\사용자ID\ , 맥 예:
/Users/사용자이름/ ) 에서 압축 해제 후, 압축 해제한 폴더를 다음 위치로 이동합니다

참고로, 최신 버전은 https://github.com/n8n-io/self-hosted-ai-starter-kit 주소로 이동하여, 초록색
Code 버튼을 클릭하고 Download ZIP을 선택하고, 다운로드 받을 수 있습니다.

참고: n8n 종료하기
n8n 사용을 마치고 싶을 때는 터미널에서 다음 명령어를 실행합니다:
docker compose down

다시 시작하려면:
docker compose up -d

이제 로컬과 외부에서 모두 접속 가능한 n8n AI 환경이 준비되었습니다!


n8n 데이터 구조 이해하기
n8n을 사용하다 보면 "왜 내 데이터가 다음 노드로 전달되지 않지?", "에러는 없는데 결과가 안 나와!" 같은 상황을 자주 마주
치게 됩니다. 이런 문제의 90%는 n8n의 데이터 구조를 제대로 이해하지 못해서 발생합니다. 이번 챕터에서는 n8n의 데이
터 구조를 실습을 통해 완벽히 이해해보겠습니다.

1. n8n 데이터 구조의 핵심
기본 규칙: 모든 것은 JSON 배열
n8n에서 모든 데이터는 특별한 형식을 따라야 합니다:
[
   {
       "json": {
          // 여기에 실제 데이터가 들어갑니다
      }
   }
]

이 구조를 분해해보면:
배열 [] : 가장 바깥쪽 (여러 아이템 처리 가능)
객체 {} : 각 아이템
"json" 키: 실제 데이터를 담는 컨테이너

왜 이런 구조일까?
1. 일관성: 모든 노드가 같은 형식으로 데이터를 주고받습니다
2. 확장성: 바이너리 파일, 에러 정보 등을 추가로 담을 수 있습니다
3. 병렬 처리: 여러 아이템을 한 번에 처리할 수 있습니다

아이템 개수에 따른 처리 방식
n8n에서 노드가 데이터를 처리하는 방식은 배열 내 아이템 개수에 따라 달라집니다:

1. 단일 아이템 (배열에 객체 1개)
[
   {
      "json": {
         "name": "김철수",
         "email": "kim@example.com"
      }
   }
]
→ 다음 노드는 이 데이터를 한 번만 처리합니다.

2. 복수 아이템 (배열에 객체 여러 개)
[
   {
      "json": {
         "name": "김철수",
         "email": "kim@example.com"
      }
   },
   {
      "json": {
         "name": "이영희",
         "email": "lee@example.com"
      }
   }
]
→ 다음 노드는 각 아이템에 대해 개별적으로 처리합니다.
첫 번째 실행: 김철수 데이터 처리
두 번째 실행: 이영희 데이터 처리

실제 예시: 이메일 발송
Gmail 노드를 예로 들어보겠습니다:

케이스 1: 한 명에게 이메일
[
   { "json": { "to": "kim@example.com", "subject": "안녕하세요" } }
]
→ Gmail 노드가 1번 실행되어 1개의 이메일 발송

케이스 2: 세 명에게 이메일
[
   { "json": { "to": "kim@example.com", "subject": "안녕하세요" } },
   { "json": { "to": "lee@example.com", "subject": "반갑습니다" } },
   { "json": { "to": "park@example.com", "subject": "환영합니다" } }
]
→ Gmail 노드가 3번 실행되어 3개의 이메일 발송

중요: 이것이 바로 n8n의 "아이템 기반 처리" 방식입니다. 각 아이템은 독립적으로 처리되므로, 한 아이템에서 오류가 발생
해도 다른 아이템은 정상 처리될 수 있습니다.

n8n UI의 데이터 표시 방식 이해하기
많은 초보자들이 혼란스러워하는 부분이 있습니다. "분명 데이터는 [{ "json": {...} }] 형식이라고 했는데, 왜 UI에서
는 json 이 보이지 않나요?"

[
  {
     "message": "Hello n8n!"
  }  
]

UI가 데이터를 숨기는 이유
n8n의 UI는 사용자 편의를 위해 실제 데이터 구조의 일부를 자동으로 숨깁니다.

실제 데이터 구조:
[
   {
      "json": {
         "data": ["kim@example.com", "lee@example.com"]
      }
   }
]

UI에서 보이는 모습:
{
   "data": [
      "kim@example.com",
      "lee@example.com"
   ]
}

왜 이렇게 할까요?
1. 가독성: json 래퍼를 매번 보여주면 실제 데이터를 파악하기 어렵습니다
2. 간결함: 중요한 것은 실제 데이터이지, 구조적 래퍼가 아닙니다
3. 일관성: 모든 노드가 같은 구조를 사용하므로 굳이 매번 보여줄 필요가 없습니다

2. 실습으로 이해하는 데이터 구조
이제 직접 워크플로우를 만들어보며 데이터 구조를 체험해봅시다.

실습 1: 첫 번째 데이터 만들기
목표: Trigger manually와 Set 노드로 기본 데이터 구조 만들기

1. 새 워크플로우 생성
n8n 화면에서 "New Workflow" 클릭

2. Trigger manually 노드 추가
좌측 패널에서 "Trigger manually" 검색
캔버스에 드래그 앤 드롭

3. Set 노드 (Edit Fields (Set)) 추가 및 연결
"Set" 노드 검색하여 추가
Trigger manually에서 Set 노드로 선 연결

4. Set 노드 설정
노드 더블클릭하여 설정 창 열기
"Add Field" 클릭
Field Name: message
Value: Hello n8n!

5. 실행 및 확인
"Execute Workflow" 버튼 클릭
Set 노드의 출력 확인

예상 결과:
# UI 상에서는
[
   {
      "message": "Hello n8n!"
   }
]

# 실제로는
[
   {
      "json": {
         "message": "안녕하세요, n8n!"
      }
   }
]

확인 포인트: n8n의 출력 데이터는 Schema, Table, JSON 세 가지 방식으로 볼 수 있습니다. 각 뷰의 특징을 이해하고
상황에 맞게 활용해보세요.

n8n의 세 가지 데이터 뷰
1. Schema View:
데이터의 필드 구조를 간단하게 보여줍니다
데이터 구조를 빠르게 파악할 때 유용합니다

2. Table View:
데이터를 엑셀과 같은 표 형태로 보여줍니다
여러 아이템을 한눈에 비교하기 좋습니다

3. JSON View:
데이터를 JSON 형식으로 보여줍니다
데이터 구조를 정확히 파악할 때 유용합니다

실전 팁:
데이터 구조 빠른 확인 시: Schema View
데이터 분석 시: Table View
API 연동 시: JSON View
를 주로 사용하면 효율적입니다.

실습 2: 여러 아이템 다루기
목표: 여러 개의 데이터 아이템을 만들고 처리하기

1. Code 노드 추가
Trigger manually 다음에 Code 노드 연결

2. 코드 작성
# 팀원 목록 만들기
team_members = [
  {"name": "김철수", "role": "개발자", "experience": 3},
  {"name": "이영희", "role": "디자이너", "experience": 5},
  {"name": "박민수", "role": "기획자", "experience": 2}
]

# n8n 형식으로 변환
# team_members 리스트의 각 member에 대해 {"json": member} 형태의 딕셔너리를 만들고,
# 이 딕셔너리들을 요소로 하는 새로운 리스트를 반환합니다.
# 파이썬의 리스트 컴프리헨션(list comprehension) 문법입니다.
return [{"json": member} for member in team_members]

중요한 발견: 실제로는 return team_members 만 작성해도 정상 동작합니다!
# 이렇게 간단하게 작성해도 됩니다
return [
{"name": "김철수", "role": "개발자", "experience": 3},
{"name": "이영희", "role": "디자이너", "experience": 5},
{"name": "박민수", "role": "기획자", "experience": 2}
]

왜 그럴까요?
n8n의 Code 노드는 내부적으로 자동으로 {"json": ...} 형식으로 래핑해줍니다
개발자가 편의를 위해 간단한 객체 배열만 반환해도 n8n이 알아서 처리합니다
하지만 데이터 구조를 명확히 이해하기 위해서는 전체 구조를 아는 것이 중요합니다

3. Code 노드 실행 모드 이해하기
Code 노드를 선택하면 오른쪽 설정 패널에서 Mode 옵션을 확인할 수 있습니다:

Run Once for All Items (모든 아이템에 대해 한 번 실행):
- 이전 노드에서 받은 모든 데이터를 한 번에 처리합니다
- items 배열로 모든 데이터에 접근 가능합니다
- 여러 아이템을 생성하거나 집계 작업에 적합합니다
- 현재 실습에서 사용하는 모드입니다

Run Once for Each Item (각 아이템마다 한 번씩 실행):
- 이전 노드에서 받은 각 아이템에 대해 개별적으로 실행됩니다
- $json 변수로 현재 아이템의 데이터에 접근합니다
- 각 아이템을 개별적으로 변환하거나 처리할 때 적합합니다

언제 어떤 모드를 사용할까요?
Run Once for All Items 사용 시기:
여러 개의 새로운 아이템을 생성할 때 (현재 실습처럼)
모든 아이템을 합쳐서 하나의 결과를 만들 때
아이템들 간의 비교나 집계가 필요할 때
Run Once for Each Item 사용 시기:
각 아이템을 개별적으로 변환할 때
각 아이템에 동일한 로직을 적용할 때
API 호출처럼 아이템별로 독립적인 작업을 할 때

4. 실행 및 확인
Code 노드를 선택한 상태에서 "Execute Node" 버튼 클릭
또는 전체 워크플로우를 실행하려면 "Test Workflow" 버튼 클릭
3개의 아이템이 생성됨을 확인
각 아이템을 클릭해 내용 확인

5. PIN 기능 활용하기
PIN이란?: 노드의 실행 결과를 고정하여 다음 실행 시에도 동일한 데이터를 사용할 수 있게 하는 기능입니다.

PIN 사용법:
Code 노드 실행 후 오른쪽 상단의 PIN 아이콘 ( ) 클릭
PIN이 활성화되면 노드 좌상단에 작은 핀 아이콘이 표시됩니다
이제 워크플로우를 다시 실행해도 이 노드는 실행되지 않고 PIN된 데이터를 사용합니다

PIN의 장점:
개발 효율성: 복잡한 데이터 생성 로직을 매번 실행하지 않아도 됩니다
테스트 일관성: 동일한 테스트 데이터로 다음 노드들을 반복 테스트할 수 있습니다
디버깅 편의성: 특정 노드의 결과를 고정하고 다음 단계만 수정/테스트할 수 있습니다

PIN 해제 방법:
노드를 선택하고 다시 PIN 아이콘을 클릭하거나
노드 좌상단의 핀 아이콘을 클릭하여 해제

실전 활용 팁
API 호출 노드: 외부 API 호출 결과를 PIN하여 API 호출 횟수를 줄이고 개발 속도 향상
복잡한 데이터 변환: 시간이 오래 걸리는 데이터 처리 결과를 PIN하여 다음 단계 개발에 집중
테스트 데이터: 일관된 테스트 환경을 위해 샘플 데이터를 PIN으로 고정
주의사항: 실제 운영 환경에서는 PIN을 해제해야 실시간 데이터가 처리됩니다.

실습 3: 단순 배열의 함정과 해결
목표: 단순 배열이 왜 문제가 되는지 이해하고 해결하기

1. 문제 상황 재현
◌ 새로운 Set 노드 추가
◌ Field Name: emails
◌ Type: Array
◌ Value: ["kim@example.com", "lee@example.com", "park@example.com"]

문제점: 3개의 이메일이 하나의 아이템 안에 배열로 들어있습니다. 각 이메일에 대해 개별 작업(예: 이메일 발송)을 하려
면 어떻게 해야 할까요?

1. 해결 방법 1: Split Out 노드 사용
◌ Split Out 노드 추가 및 연결
◌ Field to Split: emails
◌ 실행하면 3개의 개별 아이템으로 분리

결과:
INPUT
[
  {
     "emails" : [
       "kim@example.com", 
       "lee@example.com",
       "park@example.com"
     ]
  }
]

OUTPUT
[
  {
     "emails" : "kim@example.com"
  }, 
  {  
     "emails" : "lee@example.com"
  }, 
  {  
     "emails" :  "park@example.com"
  }
]

2. 해결 방법 2: Code 노드로 처리
n8n Code 노드에서 이전 노드 데이터 접근 방법
n8n의 Code 노드(Python)에서는 이전 노드의 데이터에 다음과 같이 접근할 수 있습니다:
◌ _input.all() : 모든 입력 아이템을 리스트로 반환
◌ _input.first() : 첫 번째 아이템만 반환 (단일 아이템 처리 시)
◌ _input.item : 현재 처리 중인 아이템 (각 아이템별로 실행될 때)

데이터 구조: 각 아이템은 {'json': {...}} 형태로 구성됩니다.

# 이전 노드에서 받은 모든 데이터 가져오기
input_data = _input.all()

# 첫 번째 아이템에서 emails 배열 추출
# _input.all() 은 리스트로 반환하므로, 단일 아이템이라도 [0] 을 넣어줘야 함
emails = input_data[0]['json']['emails']

# 각 이메일을 개별 아이템으로 변환
result = []
for email in emails:
  result.append({
    'json': {
      'email': email
    }
  })

return result

또는 더 간단하게:

# 단일 아이템이므로, 첫 번째 아이템만 가져와도 됨
first_item = _input.first()
emails = first_item['json']['emails']

# 리스트 컴프리헨션으로 간단하게 변환
return [{'json': {'email': email}} for email in emails]

또는 _input.item 사용 (단일 아이템일 때):

# 단일 아이템일 때 가장 직관적인 방법
first_item = _input.item
emails = first_item['json']['emails']
# 리스트 컴프리헨션으로 간단하게 변환
return [{'json': {'email': email}} for email in emails]


실습 4: 실전 미니 프로젝트 - 팀원 환영 이메일
목표: 데이터 구조를 활용한 실제 워크플로우 만들기

시나리오: 신입 팀원들의 정보를 받아 환영 메시지를 생성하는 워크플로우

1. 워크플로우 구성
◌ Trigger manually → Code (데이터 생성) → Set (메시지 추가) → IF (조건 분기)

2. Code 노드: 팀원 데이터 생성
new_members = [
{
"name": "김신입",
"email": "kim@company.com",
"department": "개발팀",
"startDate": "2024-01-15"
},
{
"name": "이신입",
"email": "lee@company.com",
"department": "마케팅팀",
"startDate": "2024-01-15"
}
]
return [{"json": member} for member in new_members]

3. Set 노드: 환영 메시지 추가
◌ Include Other Input Fields 를 true 로 설정
◌ Add Field → welcomeMessage

Value:
{{ $json.department }}에 오신 {{ $json.name }}님, 환영합니다!
입사일: {{ $json.startDate }}

4. IF 노드: 부서별 분기
- Condition: {{ $json.department }}
- Operation: Contains
- Value: 개발
- 결과 확인
◌ 각 노드의 출력을 확인하며 데이터가 어떻게 변형되는지 관찰
◌ True/False 분기에 따라 데이터가 나뉘는 것 확인



Loop Over Items로 반복 작업 마스터하기
n8n의 데이터 구조를 이해했다면, 이제 Loop Over Items 노드를 배울 준비가 되었습니다! 이 노드는 n8n에서 가장 강력
한 기능 중 하나로, 각 아이템에 대해 복잡한 작업을 반복 수행할 수 있게 해줍니다.

1. Loop Over Items가 필요한 이유
일반적인 n8n 처리 vs Loop Over Items
일반적인 n8n 처리:
데이터 → 노드 → 결과

- 각 아이템이 노드를 한 번씩 통과
- 간단한 변환이나 필터링에 적합

Loop Over Items 처리:
데이터 → Loop Over Items → [복잡한 워크플로우] → 결과

- 각 아이템에 대해 여러 노드로 구성된 복잡한 로직 실행
- API 호출, 조건부 처리, 데이터 변환 등을 조합할 때 필수

언제 Loop Over Items를 사용할까?
1. 각 아이템마다 API 호출이 필요할 때
◌ 사용자 목록 → 각 사용자의 상세 정보 조회
◌ 상품 목록 → 각 상품의 재고 확인

2. 복잡한 조건부 처리가 필요할 때
◌ 각 주문에 대해 상태별로 다른 처리
◌ 각 파일에 대해 형식별로 다른 변환

3. 외부 서비스와의 상호작용이 필요할 때
◌ 각 이미지를 다른 서비스에 업로드
◌ 각 문서를 AI 서비스로 분석

2. 실습 1: 가장 간단한 Loop Over Items
목표: Loop Over Items의 기본 동작 원리 이해하기

단계 1: 기본 데이터 준비
1. 새 워크플로우 생성
"New Workflow" 클릭
이름: "008_N8N_LOOPBASIC"

2. Trigger manually 노드 추가

3. Code 노드 추가 및 설정

4. 실행 및 확인
Code 노드 실행
3개의 아이템이 생성됨을 확인
각 아이템의 구조 파악
단계 2: Loop Over Items 노드 추가
1. Loop Over Items 노드 검색 및 추가
Code 노드 다음에 연결
노드 패널에서 "Loop Over Items" 검색
2. 기본 설정 확인
Batch Size: 1 (기본값)
Loop Over Items 설정 이해하기
Batch Size가 1일 때와 여러 개일 때의 차이점:
# 간단한 과일 목록 생성
fruits = [
{"name": "사과", "color": "빨강", "price": 1000},
{"name": "바나나", "color": "노랑", "price": 800},
{"name": "포도", "color": "보라", "price": 1500}
]
return fruits
Batch Size = 1 (권장):
각 실행마다 하나의 아이템만 처리
{{ $json.name }} 은 항상 하나의 값 (예: "사과")
예측 가능하고 디버깅 쉬움
Batch Size = 3일 때:
한 번에 여러 아이템이 배열로 들어옴
{{ $json.name }} 이 배열이 될 수 있음 (예: ["사과", "바나나", "포도"])
루프 내부 노드들이 배열을 어떻게 처리할지 예측하기 어려움
왜 복잡해질까요?
1. 데이터 구조 변화:
Batch Size = 1: 각 노드가 단일 객체 처리
Batch Size = 3: 각 노드가 배열 처리 (예상과 다를 수 있음)
2. 에러 처리 복잡성:
Batch Size = 1: 한 아이템 실패 → 명확한 원인 파악
Batch Size = 3: 3개 중 일부 실패 → 어떤 아이템이 문제인지 찾기 어려움
초보자 권장: Batch Size는 1 로 시작하세요!
단계 3: 루프 내부에 간단한 로직 추가
1. Set 노드 추가
Loop Over Items 내부에 Set 노드 추가
루프 내부임을 확인 (노드가 Loop 박스 안에 있어야 함)
2. Set 노드 설정
Include Other Input Fields: true (기존 데이터 유지)
Add Field 클릭
실행 1: 사과 → Set 노드 실행 → 결과: 1개
실행 2: 바나나 → Set 노드 실행 → 결과: 1개
실행 3: 포도 → Set 노드 실행 → 결과: 1개
실행 1: [사과, 바나나, 포도] → Set 노드 실행 → 결과: 3개
Name: description
Value: {{ $json.color }} 색깔의 {{ $json.name }}이고, 가격은 {{ $json.price }}원입니다.
3. 실행 및 결과 확인
Loop Over Items 노드 실행
각 과일에 대해 설명이 추가됨을 확인
예상 결과:
중요한 발견: Loop Over Items 내부에서도 {{ $json.필드명 }} 으로 현재 아이템의 데이터에 접근할 수 있습니다!
3 실습 2: 조건부 처리가 있는 루프
목표: 루프 내부에서 IF 노드를 사용한 조건부 처리
단계 1: 기존 워크플로우 확장
1. Set 노드 다음에 IF 노드 추가
Loop Over Items 내부에 IF 노드 추가
2. IF 노드 설정
[
{
"name": "사과",
"color": "빨강",
"price": 1000,
"description": "빨강 색깔의 사과이고, 가격은 1000원입니다."
},
{
"name": "바나나",
"color": "노랑",
"price": 800,
"description": "노랑 색깔의 바나나이고, 가격은 800원입니다."
},
{
"name": "포도",
"color": "보라",
"price": 1500,
"description": "보라 색깔의 포도이고, 가격은 1500원입니다."
}
]
본 자료는 저작권법 제25조 2항에 의해 보호를 받으며, pdf 형태로 보실 수 있도록 공유드리고 있습니다.
본 자료를 인터넷상에 오픈 또는 공유하시면, 쉽게 확인이 되어 저작권법에 의해 부득이 조치를 취할 수 밖에 없으므로, 인터넷상에 오픈
또는 공유는 하지 말아주세요
마지막으로 풀스택 로드맵과 함께 익히시면, 보다 자유자재로 n8n 기술을 익히고 활용할 수 있습니다
관련 강의도 잔재미코딩(노이먼스랩) 에서 확인하실 수 있으며,
해당 사이트에서 회원가입하시면, 최신 AI 및 자동화, 데이터, 풀스택 관련 트렌드 뉴스레터도 공유받으실 수 있습니다.
Condition: {{ $json.price }}
Operation: >
Value: 1000
단계 2: 조건별 다른 처리
1. True 브랜치: 비싼 과일 처리
IF 노드의 True 출력에 Set 노드 추가
Include Other Input Fields 는 true 로 설정
Name: category , Value: 프리미엄 추가
2. False 브랜치: 저렴한 과일 처리
IF 노드의 False 출력에 Set 노드 추가
Include Other Input Fields 는 true 로 설정
Name: category , Value: 일반 추가
3. 실행 및 결과 확인
가격에 따라 다른 카테고리가 할당됨을 확인
예상 결과:
사과 (1000원): 일반 (1000원은 초과가 아니므로)
바나나 (800원): 일반
포도 (1500원): 프리미엄
Loop Over Items의 핵심: 각 아이템이 독립적으로 전체 루프 로직을 거쳐갑니다. 마치 각 아이템이 개별적인 워크플
로우를 실행하는 것과 같습니다!



